
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Dossiers natifs générés par `expo prebuild` / `expo run:ios` (workflow
# managed : ils sont reconstruits à la demande et ne doivent pas être
# versionnés — 1,2 Go, et leur contenu dépend de la machine).
ios/
android/

# Clé du compte de service Google Play, utilisée par `eas submit` (cf.
# eas.json submit.production.android) : c'est un secret qui donne le droit de
# publier sur le Play Store, il ne doit jamais partir dans le dépôt.
google-play-service-account.json
