{
  "name": "cheval",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "mobile": "pnpm --filter @cheval/mobile dev",
    "api": "pnpm --filter @cheval/api dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:generate": "turbo db:generate",
    "db:push": "turbo db:push",
    "db:studio": "turbo db:studio"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  },
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  }
}
